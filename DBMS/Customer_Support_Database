
CREATE DATABASE IF NOT EXISTS customer_support_simple;
USE customer_support_simple;

-- DDL: CREATE TABLE
CREATE TABLE customer (
    customer_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    phone VARCHAR(20),
    city VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE product (
    product_id INT AUTO_INCREMENT PRIMARY KEY,
    product_name VARCHAR(100) NOT NULL,
    category VARCHAR(50),
    price DECIMAL(10,2) NOT NULL,
    stock INT DEFAULT 0
);

CREATE TABLE chat_session (
    session_id INT AUTO_INCREMENT PRIMARY KEY,
    customer_id INT,
    session_date DATE,
    rating INT,
    status VARCHAR(20) DEFAULT 'active',
    FOREIGN KEY (customer_id) REFERENCES customer(customer_id)
);

CREATE TABLE message (
    message_id INT AUTO_INCREMENT PRIMARY KEY,
    session_id INT,
    sender VARCHAR(20),
    message_text TEXT,
    FOREIGN KEY (session_id) REFERENCES chat_session(session_id)
);

CREATE TABLE support_ticket (
    ticket_id INT AUTO_INCREMENT PRIMARY KEY,
    customer_id INT,
    issue_description TEXT NOT NULL,
    status VARCHAR(20) DEFAULT 'open',
    priority VARCHAR(20) DEFAULT 'medium',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (customer_id) REFERENCES customer(customer_id)
);

-- DML: INSERT
INSERT INTO customer (name, email, phone, city)
VALUES
('Ashish', 'ashish@gmail.com', '9876543210', 'Pune'),
('Priya', 'priya@gmail.com', '9123456780', 'Mumbai'),
('Rahul', 'rahul@gmail.com', '9988776655', 'Nagpur');

INSERT INTO product (product_name, category, price, stock)
VALUES
('Laptop', 'Electronics', 45000.00, 20),
('Mobile', 'Electronics', 22000.00, 35),
('Headphones', 'Accessories', 2500.00, 50),
('Keyboard', 'Accessories', 1800.00, 40);

INSERT INTO chat_session (customer_id, session_date, rating, status)
VALUES
(1, '2026-09-01', 5, 'closed'),
(2, '2026-09-02', 4, 'active'),
(3, '2026-09-03', 3, 'closed');

INSERT INTO message (session_id, sender, message_text)
VALUES
(1, 'customer', 'I need help with my order.'),
(1, 'agent', 'Sure, I can help you with that.'),
(2, 'customer', 'Can you suggest a good laptop?'),
(3, 'agent', 'Your ticket has been raised.');

INSERT INTO support_ticket (customer_id, issue_description, status, priority)
VALUES
(1, 'Order delivery delayed', 'open', 'high'),
(2, 'Need help with product return', 'in_progress', 'medium'),
(3, 'Login issue', 'closed', 'low');

-- DML: SELECT
SELECT * FROM customer;
SELECT * FROM product;
SELECT * FROM chat_session;
SELECT * FROM message;
SELECT * FROM support_ticket;

-- DML: UPDATE
UPDATE customer
SET city = 'Delhi'
WHERE customer_id = 3;

UPDATE product
SET price = 20000.00
WHERE product_name = 'Mobile';

-- DML: DELETE
DELETE FROM message
WHERE message_id = 4;

-- DDL: ALTER TABLE
ALTER TABLE customer
ADD COLUMN gender VARCHAR(10);

ALTER TABLE product
ADD COLUMN warranty_years INT;

-- DDL: DROP TABLE (optional practice)
-- DROP TABLE message;

-- SELECT AFTER UPDATE
SELECT * FROM customer;
SELECT * FROM product;

-- JOIN QUERY EXAMPLE
SELECT c.customer_id, c.name, s.session_id, s.status
FROM customer c
JOIN chat_session s ON c.customer_id = s.customer_id;

-- Another query example
SELECT t.ticket_id, c.name, t.issue_description, t.status
FROM support_ticket t
JOIN customer c ON t.customer_id = c.customer_id;

-- Practice DDL commands you can try:
-- CREATE TABLE new_table (...);
-- ALTER TABLE customer RENAME COLUMN city TO location;
-- DROP TABLE support_ticket;

-- Practice DML commands you can try:
-- INSERT INTO customer (name, email, phone, city) VALUES ('Amit', 'amit@gmail.com', '9870001234', 'Jaipur');
-- UPDATE support_ticket SET status = 'closed' WHERE ticket_id = 1;
-- DELETE FROM product WHERE product_id = 4;
